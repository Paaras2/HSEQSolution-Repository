# استقرار سامانه مدیریت یکپارچه مدارک و مستندات (HSEQ)

این سند همان چیزی را توصیف می‌کند که واقعاً در مخزن پیاده شده، نه یک معماری آرمانی.

---

## ۱. چیدمان

سامانه از دو بخش تشکیل شده که روی **یک سایت IIS و یک مبدأ** سرو می‌شوند:

```
https://hseq.odcc.local/            →  فایل‌های استاتیک React   (Frontend\)
https://hseq.odcc.local/api/...     →  ASP.NET Core Web API     (Backend\)
```

بک‌اند به‌صورت یک **Application** با مسیر `/api` زیر همان سایت ثبت می‌شود.

> ### تکرار پیشوند `api` — حل شده
>
> کنترلرها با `[Route("api/[controller]")]` نوشته شده‌اند و بک‌اند هم زیر مسیر
> `/api` ثبت می‌شود. IIS آن پیشوند را به‌عنوان `PathBase` از مسیر برمی‌دارد، پس
> درخواستِ `/api/Search` به مسیرِ `/Search` می‌رسید و با هیچ کنترلری جور در
> نمی‌آمد — یعنی **همه‌ی فراخوان‌های کلاینت ۴۰۴ می‌گرفتند**.
>
> یک میان‌افزار در `Program.cs` همان پیشوند را به مسیر برمی‌گرداند، پس مسیریابی در
> هر دو چیدمان یکسان رفتار می‌کند و در محیط توسعه (که `PathBase` خالی است) کاملاً
> بی‌اثر است.
>
> با شبیه‌سازی همان شرایط روی ماشین توسعه آزمایش شد:
>
> | مسیر | بدون PathBase | با `PathBase=/api` |
> |---|---|---|
> | `/api/Health` | ۲۰۰ | ۲۰۰ |
> | `/api/MasterData/projects` | ۴۰۱ | ۴۰۱ |
>
> `Deploy-Production.ps1` همچنان هر دو نشانی را می‌آزماید تا اگر روزی این رفتار
> برگشت، استقرار با پیام صریح متوقف شود.

چرا هم‌مبدأ: کلاینت Vite است و آدرس API را در **زمان build** داخل فایل‌های خروجی
می‌نویسد. با مسیر نسبی `/api`، خروجی build به هیچ دامنه‌ای گره نمی‌خورد؛ همان فایل‌ها
روی هر نامی کار می‌کنند و تغییر دامنه build مجدد لازم ندارد.

| مؤلفه | فناوری |
|---|---|
| بک‌اند | ASP.NET Core 10 (`net10.0`) |
| دسترسی داده | EF Core 8.0.28، Code-First |
| دیتابیس | SQL Server، دیتابیس `HSEQDb` |
| کلاینت | React 19 + TypeScript + Vite 7 |
| احراز هویت | JWT Bearer (HMAC-SHA256) |
| اعتبارسنجی کاربر | سرویس بیرونی User Management |

---

## ۲. پیش‌نیازهای سرور

روی سرور **هیچ‌کدام از این‌ها لازم نیست**: .NET SDK، Node.js، npm، کد منبع.

لازم است:

| پیش‌نیاز | توضیح |
|---|---|
| IIS | با نقش Web Server |
| **ASP.NET Core 10 Hosting Bundle** | ماژول `AspNetCoreModuleV2` را نصب می‌کند |
| **URL Rewrite Module** | بدون آن رفرش روی `/documents` خطای ۴۰۴ می‌دهد |
| SQL Server | دسترسی از سرور IIS روی پورت ۱۴۳۳ |
| دسترسی به سرویس UM | پورت خروجی ۸۰۳۰ به `172.17.0.254` |

سرور ممکن است اینترنت نداشته باشد. اسکریپت استقرار **هیچ‌چیزی دانلود نمی‌کند**؛ اگر
پیش‌نیازی نباشد، دقیقاً نامش را گزارش می‌دهد تا آفلاین نصبش کنید.

> **هشدار:** بدون دسترسی به سرویس UM، هیچ کاربری نمی‌تواند وارد شود — حتی اگر سامانه
> و دیتابیس کاملاً سالم باشند. این را در قوانین فایروال لحاظ کنید.

---

## ۳. ساخت بسته

روی ماشین توسعه (نه سرور):

```powershell
.\Publish-Production.ps1
```

خروجی: `artifacts\HSEQ_<yyyyMMdd-HHmmss>\`

```
Backend\      خروجی dotnet publish (Release) + web.config
Frontend\     فایل‌های استاتیک کلاینت + web.config مخصوص مسیریابی SPA
Database\     migrations.sql — اسکریپت idempotent
Scripts\      Deploy-Production.ps1
Config\       appsettings.Production.template.json
release.json  فراداده‌ی نسخه
SHA256SUMS.txt
```

اسکریپت fail-fast است: هر گام که شکست بخورد، کد خروجی غیرصفر می‌دهد.

سوئیچ‌ها: `-SkipFrontend` (فقط بک‌اند)، `-SkipDatabase` (بدون اسکریپت مهاجرت).

---

## ۴. تنظیمات زمان اجرا

فایل `appsettings.Production.json` **روی سرور** کنار `HSEQ.API.dll` می‌ماند و
**هرگز در گیت commit نمی‌شود** (در `.gitignore` هست). استقرار مجدد آن را حفظ می‌کند.

از `Config\appsettings.Production.template.json` کپی بگیرید.

### کلیدهای اجباری

| کلید | توضیح |
|---|---|
| `ConnectionStrings:DefaultConnection` | ترجیحاً احراز هویت یکپارچه ویندوز تا رمز در فایل نباشد |
| `Jwt:Key` | **باید تازه ساخته شود** — پایین را بخوانید |
| `Jwt:Issuer` / `Jwt:Audience` | `OdccPM` / `Audience` |
| `Jwt:ExpiryInMinutes` | عدد صحیح مثبت (فعلاً ۶۰) |
| `UploadPath` | مسیر ذخیره‌ی فایل مدارک، بیرون از پوشه‌ی نسخه |
| `UserManagementAPI:Url` | نشانی سرویس احراز هویت |
| `Cors:AllowedOrigins` | دامنه‌ای که کلاینت از آن سرو می‌شود |

### کلیدهای اختیاری

| کلید | پیش‌فرض | توضیح |
|---|---|---|
| `Database:MigrateOnStartup` | `false` بیرون از توسعه | مهاجرت هنگام بالا آمدن |
| `Database:SeedOnStartup` | `true` | upsert داده‌ی پایه؛ اجرای دوباره بی‌خطر |

### جایگزین امن‌تر برای اسرار

به‌جای نوشتن در فایل، متغیر محیطی روی Application Pool تعریف کنید (دو زیرخط):

```
Jwt__Key
ConnectionStrings__DefaultConnection
```

### ⚠ کلید JWT فعلی سوخته است

کلید داخل `appsettings.json` مخزن در تاریخچه‌ی گیت است. هر کسی که به مخزن دسترسی
دارد می‌تواند با آن توکن جعلی بسازد. برنامه در محیط عملیاتی این کلید را **رد می‌کند**
و بالا نمی‌آید.

کلید تازه:

```powershell
[Convert]::ToBase64String((1..48 | ForEach-Object { Get-Random -Maximum 256 }))
```

با عوض شدن کلید، توکن‌های فعال باطل می‌شوند و کاربران باید دوباره وارد شوند.

---

## ۵. استقرار روی سرور

بسته را به سرور منتقل کنید، سپس در PowerShell **با دسترسی Administrator**:

```powershell
.\Deploy-Production.ps1 -PackagePath "C:\releases\HSEQ_20260823-180056" -DryRun
```

`-DryRun` هیچ تغییری اعمال نمی‌کند و فقط پیش‌نیازها را بررسی می‌کند. **همیشه اول این.**

پس از سبز شدن همه‌ی بررسی‌ها:

```powershell
.\Deploy-Production.ps1 -PackagePath "C:\releases\HSEQ_20260823-180056" -ApplySchema -SqlServer "<SQL-HOST>"
```

بدون `-ApplySchema` دیتابیس اصلاً دست نمی‌خورد.

### ترتیب کار اسکریپت

اول **همه‌ی بررسی‌ها**، بعد تغییرات — تا استقرار نیمه‌کاره پیش نیاید:

1. دسترسی مدیر · صحت SHA-256 بسته · پیش‌نیازهای IIS · زمان اجرای .NET
2. وجود فایل تنظیمات · تداخل پورت · اتصال SQL
3. **اگر هر بررسی شکست بخورد، اینجا متوقف می‌شود**
4. پشتیبان‌گیری دیتابیس → اعمال مهاجرت
5. نسخه‌برداری از استقرار فعلی → کپی فایل‌ها → تنظیم ACL
6. پیکربندی IIS → ری‌سایکل فقط app pool همین سایت
7. بررسی سلامت

اسکریپت هرگز IIS را به‌صورت کلی ری‌استارت نمی‌کند، سایت دیگری را دست نمی‌زند و پورت
سایت دیگری را نمی‌گیرد.

---

## ۶. مسیرها

| مسیر | محتوا |
|---|---|
| `C:\Applications\HSEQ\current\frontend\` | فایل‌های استاتیک کلاینت |
| `C:\Applications\HSEQ\current\api\` | بک‌اند + `appsettings.Production.json` |
| `C:\ApplicationData\HSEQ\Documents\` | **فایل مدارک** |
| `C:\ApplicationData\HSEQ\logs\` | لاگِ stdout ماژول ASP.NET Core |
| `C:\ApplicationData\HSEQ\DbBackups\` | پشتیبان‌های پیش از مهاجرت |
| `C:\ApplicationData\HSEQ\rollback\<timestamp>\` | نسخه‌ی قبلی برنامه |

داده‌های ماندگار عمداً **بیرون از پوشه‌ی نسخه** هستند تا استقرار بعدی پاکشان نکند.

فقط `Documents\` و `logs\` دسترسی نوشتن می‌گیرند (`Modify` برای `IIS AppPool\HSEQ`).

> **پشتیبان‌گیری:** فایل مدارک داخل دیتابیس نیست. پشتیبان SQL به‌تنهایی کافی نیست —
> `C:\ApplicationData\HSEQ\Documents\` هم باید در برنامه‌ی پشتیبان‌گیری باشد.

### لاگ و عیب‌یابی

`HSEQ.API\web.config` مسیر لاگ stdout را روی `C:\ApplicationData\HSEQ\logs\stdout`
تنظیم می‌کند — عمداً بیرون از پوشه‌ی برنامه، چون لاگِ عیب‌یابی دقیقاً بعد از یک
استقرارِ خراب لازم می‌شود و نباید با استقرار بعدی پاک شود.

پیش‌فرض `stdoutLogEnabled="false"` است: این لاگ چرخش ندارد و روشن ماندنش دیسک را پر
می‌کند. برای عیب‌یابی موقتاً `true` کنید، مشکل را پیدا کنید و دوباره `false` کنید.

خطاهای مدیریت‌نشده‌ی سرور (۵۰۰) از طریق `ILogger` ثبت می‌شوند؛ به کاربر فقط پیام
عمومی برمی‌گردد و جزئیات بیرون نمی‌رود.

### Data Protection

این برنامه از حلقه‌کلیدهای Data Protection استفاده‌ی عملیاتی نمی‌کند: احراز هویت
کاملاً مبتنی بر JWT با کلید متقارنِ خودِ تنظیمات است و کوکی یا Antiforgery در کار
نیست. پس ری‌سایکل شدن Application Pool یا استقرار مجدد، هیچ نشستی را باطل نمی‌کند.
عمداً پیکربندی جداگانه‌ای برایش اضافه نشده تا زیرسیستمی که استفاده نمی‌شود، به
مسیر راه‌اندازی وابستگی اضافه نکند.

---

## ۷. دیتابیس

مهاجرت‌ها با `migrations.sql` اعمال می‌شوند: اسکریپت **idempotent** است، هر مهاجرت
داخل یک `IF NOT EXISTS` روی `__EFMigrationsHistory` می‌نشیند، پس اجرای دوباره‌اش روی
دیتابیس به‌روز بی‌اثر است. برخلاف مهاجرت هنگام بالا آمدن، می‌شود قبل از اجرا خواندش.

مهاجرت هنگام بالا آمدن بیرون از محیط توسعه **پیش‌فرض خاموش** است. قبلاً هر ری‌استارت
IIS می‌توانست ساختار دیتابیس را بدون پشتیبان و بدون بازبینی عوض کند.

**اولین استقرار:** دیتابیس خالی `HSEQDb` بسازید، سپس با `-ApplySchema` اجرا کنید.
Seed خودکار داده‌ی پایه‌ی دامنه را می‌نویسد: مدیریت‌ها، فعالیت‌ها، انواع سند و آرشیو
۷۶۸ شماره‌ی قدیمی. این داده‌ی واقعی سازمان است، نه داده‌ی آزمایشی.

---

## ۸. بررسی‌های پس از استقرار

| بررسی | انتظار |
|---|---|
| `https://hseq.odcc.local/` | صفحه‌ی ورود |
| `https://hseq.odcc.local/api/Health` | ۲۰۰ با `{"status":"Healthy"}` — یعنی برنامه **و** اتصال دیتابیس سالم است |
| `https://hseq.odcc.local/api/MasterData/projects` | ۴۰۱ (یعنی احراز هویت فعال است) |
| `https://hseq.odcc.local/documents` با رفرش | صفحه بیاید، نه ۴۰۴ ← تأیید URL Rewrite |
| `https://hseq.odcc.local/swagger` | ۴۰۴ ← مستندات در عملیاتی بسته است |
| ورود با کد پرسنلی واقعی | تأیید دسترسی به سرویس UM |
| افزودن یک سند آزمایشی | تأیید دسترسی نوشتن روی `Documents\` |
| دانلود همان سند | تأیید مسیر خواندن فایل |

---

## ۸.۱. آزمایش ورود وقتی سرویس UM در دسترس نیست

روی ماشینی که به شبکه‌ی سازمان وصل نیست، سرویس واقعی UM جواب نمی‌دهد و هیچ‌کس
نمی‌تواند وارد شود — یعنی صفحه‌ی ورود و کل جریان احراز هویت قابل آزمایش نیست.

`Start-UmSimulator.ps1` فقط همان یک اندپوینتی را که `HSEQ.API` صدا می‌زند
شبیه‌سازی می‌کند (`POST {UmUrl}Auth/checkCredential`). بقیه‌ی زنجیره واقعی می‌ماند:
توکن JWT را خودِ برنامه می‌سازد و نقش کاربر را از جدول `Admins` همان دیتابیس
می‌خواند. یعنی آنچه آزمایش می‌شود جریان واقعی ورود است، نه یک میان‌بر.

```powershell
# روی ماشین آزمایش:
.\Start-UmSimulator.ps1
```

سپس در `appsettings.Production.json` همان ماشین:

```json
"UserManagementAPI": { "Url": "http://um-sim.local:8899/api/" }
```

**چرا `localhost` نه:** اعتبارسنجی راه‌اندازی عمداً نشانی `localhost` و `127.0.0.1`
را برای سرویس UM رد می‌کند. این محافظ درست است و نباید ضعیف شود. راهش این است که
در فایل `hosts` یک نام مستعار به لوپ‌بک بسازید و شبیه‌ساز را با همان نام اجرا کنید:

```
127.0.0.1   um-sim.local
```
```powershell
# نیازمند PowerShell با دسترسی Administrator (محدودیت HttpListener)
.\Start-UmSimulator.ps1 -Hostname um-sim.local
```

کدهای پرسنلی مجاز پیش‌فرض `3256` و `3548` هستند — همان دو حسابی که در جدول
`Admins` نقش «مدیر سیستم» دارند. رمز عبور هر مقدار غیرخالی پذیرفته می‌شود.

> ### ⚠ این شبیه‌ساز هرگز نباید روی سرور عملیاتی برود
>
> هر رمزی را می‌پذیرد. تنها محافظش این است که روی لوپ‌بک گوش می‌دهد. اگر تنظیمات
> عملیاتی به آن اشاره کند، احراز هویت سامانه عملاً دور زده شده است.
>
> در بسته‌ی انتشار قرار نمی‌گیرد (`Publish-Production.ps1` فقط
> `Deploy-Production.ps1` را کپی می‌کند). پس از پایان آزمایش، `UserManagementAPI:Url`
> را حتماً به نشانی واقعی برگردانید.

---

## ۹. بازگشت به نسخه‌ی قبل

سه نوع بازگشت که نباید با هم اشتباه شوند:

**برنامه** — محتوای `C:\ApplicationData\HSEQ\rollback\<timestamp>\` را روی مسیرهای
مقصد برگردانید و app pool را ری‌سایکل کنید. استقرار قبلی هرگز پاک نمی‌شود.

**تنظیمات** — `appsettings.Production.json` روی سرور می‌ماند و استقرار آن را عوض
نمی‌کند، پس معمولاً نیازی به بازگشت ندارد.

**دیتابیس** — **خودکار انجام نمی‌شود و نباید حدس زده شود.** مهاجرت‌ها لزوماً برگشت‌پذیر
نیستند (مثلاً حذف ستون، داده‌اش را برمی‌گرداند نمی‌شود). راه بازیابی، restore از
پشتیبانی است که پیش از مهاجرت گرفته شده.

---

## ۱۰. مواردی که فقط روی سرور قابل تأیید است

این‌ها روی ماشین توسعه قابل آزمایش نبودند:

- نصب بودن IIS و ماژول‌های `AspNetCoreModuleV2` و URL Rewrite
- گواهی HTTPS و binding
- نوشتن لاگ stdout روی `C:\ApplicationData\HSEQ\logs\` با هویت Application Pool
- دسترسی هویت Application Pool روی مسیرهای داده
- رفتار واقعی URL Rewrite روی مسیرهای داخلی React
- اتصال به SQL Server عملیاتی
- دسترسی شبکه به سرویس UM و ورود واقعی کاربر
- عملکرد `Documents\` تحت هویت Application Pool
