{
  "//": [
    "قالب تنظیمات عملیاتی. این فایل را روی سرور به appsettings.Production.json تغییر نام",
    "دهید، مقادیر <...> را پر کنید و آن را کنار HSEQ.API.dll بگذارید.",
    "",
    "این فایل هرگز نباید با مقادیر واقعی داخل گیت commit شود.",
    "",
    "جایگزین امن‌تر: به‌جای نوشتن اسرار در فایل، آن‌ها را به‌صورت متغیر محیطی روی",
    "Application Pool تعریف کنید. نام متغیر با دو زیرخط ساخته می‌شود:",
    "  Jwt__Key",
    "  ConnectionStrings__DefaultConnection"
  ],

  "ConnectionStrings": {
    "//": [
      "نام کلید دقیقاً همین است و متغیر محیطیِ متناظرش ConnectionStrings__DefaultConnection.",
      "",
      "احراز هویت یکپارچه ویندوز ترجیح داده می‌شود، چون هیچ رمزی در هیچ فایلی نمی‌ماند.",
      "اگر لاگین SQL لازم است، این شکل را از راه *متغیر محیطی* بدهید، نه داخل این فایل:",
      "  Server=<SQL-SERVER-HOST>;Database=HSEQDb;User Id=<SQL-LOGIN>;Password=<SQL-PASSWORD>;TrustServerCertificate=True;Encrypt=False;MultipleActiveResultSets=true",
      "",
      "<SQL-PASSWORD> جای‌نگهدار است. مقدار واقعی هرگز در گیت، بسته، لاگ یا سند نوشته نشود.",
      "لاگین برنامه فقط db_datareader/db_datawriter لازم دارد - نه sysadmin و نه db_owner."
    ],
    "DefaultConnection": "Server=<SQL-SERVER-HOST>;Database=HSEQDb;Trusted_Connection=True;TrustServerCertificate=True;Encrypt=False;MultipleActiveResultSets=true"
  },

  "Api": {
    "//": [
      "پیشوند عمومی API. مرورگر /api/Auth/login را صدا می‌زند و همین‌جا تعیین می‌شود.",
      "زیر IIS، بک‌اند یک Application با مسیر /api است و خودِ IIS پیشوند را می‌گذارد؛",
      "برنامه فقط وقتی خودش اعمالش می‌کند که میزبان نگذاشته باشد (مثلاً اجرای مستقیم",
      "dotnet HSEQ.API.dll). این مقدار را عوض نکنید مگر آنکه مسیر Application در IIS",
      "را هم دقیقاً به همان مقدار عوض کنید - و کلاینت را با VITE_API_BASE_URL جدید",
      "دوباره build کنید."
    ],
    "PathBase": "/api"
  },

  "Https": {
    "//": [
      "هدایت HTTP به HTTPS. فقط وقتی true کنید که سایت واقعاً بایندینگ HTTPS دارد؛",
      "وگرنه هیچ هدایتی انجام نمی‌شود و فقط لاگ پر می‌شود.",
      "Port را وقتی بدهید که پورت عمومی HTTPS چیزی جز ۴۴۳ است."
    ],
    "RedirectToHttps": false
  },

  "Jwt": {
    "//": [
      "کلیدِ داخل مخزن در تاریخچه‌ی گیت لو رفته و برنامه در محیط عملیاتی آن را رد می‌کند.",
      "کلید تازه بسازید (RandomNumberGenerator، نه Get-Random - آن مولد برای رمزنگاری",
      "امن نیست و خروجی‌اش قابل پیش‌بینی است):",
      "  $b=[byte[]]::new(48); [System.Security.Cryptography.RandomNumberGenerator]::Fill($b); [Convert]::ToBase64String($b)",
      "این فایل را با مقدار واقعی هرگز در گیت commit نکنید.",
      "با عوض شدن کلید، همه‌ی توکن‌های فعال باطل می‌شوند و کاربران باید دوباره وارد شوند."
    ],
    "Key": "<NEW-BASE64-SIGNING-KEY-AT-LEAST-32-CHARS>",
    "Issuer": "OdccPM",
    "Audience": "Audience",
    "ExpiryInMinutes": 60
  },

  "UserManagementAPI": {
    "//": "بدون دسترسی به این سرویس هیچ کاربری نمی‌تواند وارد شود.",
    "Url": "http://172.17.0.254:8030/api/"
  },

  "Cors": {
    "//": "فقط دامنه‌هایی که کلاینت از آن‌ها سرو می‌شود. آرایه‌ی خالی یعنی همه.",
    "AllowedOrigins": [ "https://hseq.odcc.local" ]
  },

  "Database": {
    "//": [
      "مهاجرت در محیط عملیاتی توسط Deploy-Production.ps1 و پس از پشتیبان‌گیری اجرا",
      "می‌شود، نه هنگام بالا آمدن برنامه. این را true نکنید مگر آگاهانه.",
      "SeedOnStartup فقط داده‌ی پایه‌ی دامنه را upsert می‌کند و اجرای دوباره‌اش بی‌خطر است."
    ],
    "MigrateOnStartup": false,
    "SeedOnStartup": true
  },

  "//UploadPath": [
    "محل ذخیره‌ی فایل مدارک. بیرون از پوشه‌ی نسخه باشد، وگرنه استقرار بعدی پاکش می‌کند.",
    "هویت Application Pool باید روی این مسیر دسترسی Modify داشته باشد."
  ],
  "UploadPath": "C:\\ApplicationData\\HSEQ\\Documents\\",

  "Logging": {
    "//": [
      "Information برای استقرار تازه، تا اگر چیزی خراب بود در لاگ stdout دیده شود.",
      "پس از تثبیت به Warning برگردانید: با Information هر درخواست و هر کوئری EF",
      "یک سطر می‌نویسد و لاگِ بدون-چرخشِ stdout سریع بزرگ می‌شود.",
      "",
      "Microsoft.EntityFrameworkCore.Database.Command روی Warning می‌ماند چون",
      "در سطح Information متنِ کامل هر کوئری SQL را در لاگ می‌نویسد."
    ],
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },

  "AllowedHosts": "*"
}
